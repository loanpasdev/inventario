@page
@model InventoryManagement.Web.Pages.Monedas.IndexModel
@{
    ViewData["Title"] = "Monedas";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="display-6 mb-1 im-page-title">Monedas</h1>
        <p class="text-muted mb-0">Listado de monedas.</p>
    </div>
</div>

@if (!string.IsNullOrWhiteSpace(Model.StatusMessage))
{
    <div class="alert alert-info alert-dismissible fade show" role="alert">
        @Model.StatusMessage
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Cerrar"></button>
    </div>
}

<div class="card shadow-sm">
    <div class="card-header bg-light d-flex justify-content-between align-items-center">
        <strong>Listado</strong>
        <span class="text-muted small" data-filter-count data-count-label="moneda(s)">@Model.Currencies.Count moneda(s)</span>
    </div>
    <div class="card-body">
        @if (Model.Currencies.Count == 0)
        {
            <p class="text-muted mb-0">No hay monedas para mostrar.</p>
        }
        else
        {
            var statusFilters = Model.Currencies
                .Select(currency => currency.Status)
                .Where(status => !string.IsNullOrWhiteSpace(status))
                .Distinct(StringComparer.OrdinalIgnoreCase)
                .OrderBy(status => status)
                .ToList();
            <div data-filter-group>
                <div class="im-filter-panel mb-3">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <label class="form-label">Buscar</label>
                            <input type="text" class="form-control" placeholder="Código, nombre o estatus..." data-search-input />
                        </div>
                        <div class="col-md-6">
                            <label class="form-label">Filtrar por estatus</label>
                            <div class="dropdown im-check-combo" data-filter-target="status" data-filter-placeholder="Todos los estatus" data-bs-auto-close="outside">
                                <button class="btn btn-outline-secondary dropdown-toggle w-100" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    <span class="im-check-combo-label" data-filter-button-label>Todos los estatus</span>
                                </button>
                                <div class="dropdown-menu w-100 p-2 im-check-combo-menu">
                                    @foreach (var status in statusFilters)
                                    {
                                        <label class="dropdown-item-text im-check-option">
                                            <input class="form-check-input" type="checkbox" value="@status" data-label="@status" />
                                            <span>@status</span>
                                        </label>
                                    }
                                </div>
                            </div>
                            <small class="im-filter-help">Puedes seleccionar varios estatus.</small>
                        </div>
                    </div>
                </div>

                <div class="row row-cols-1 row-cols-md-2 row-cols-xl-4 row-cols-xxl-5 g-3">
                    @foreach (var currency in Model.Currencies)
                    {
                        var isActive = string.Equals(currency.Status, "Activo", StringComparison.OrdinalIgnoreCase);
                        <div class="col" data-filter-card data-status="@currency.Status" data-search="@($"{currency.Code} {currency.Name} {currency.Status}")">
                            <div class="card h-100 im-card im-card--compact">
                                <div class="card-body im-card-body">
                                    <div class="d-flex justify-content-between align-items-start gap-2 im-master-card-top">
                                        <div class="im-master-card-main">
                                            <h2 class="h6 mb-1 im-title">
                                                <span class="im-title-icon" aria-hidden="true">
                                                    <svg viewBox="0 0 16 16" fill="currentColor" xmlns="http://www.w3.org/2000/svg">
                                                        <path d="M8 1.5a.5.5 0 0 1 .5.5v.553c1.618.179 2.75 1.06 2.75 2.447a.5.5 0 0 1-1 0c0-.768-.757-1.396-2.25-1.486V7c2.248.214 3.25 1.21 3.25 2.75 0 1.514-1.154 2.56-2.75 2.742V13a.5.5 0 0 1-1 0v-.5c-1.813-.13-3-1.108-3-2.5a.5.5 0 0 1 1 0c0 .84.78 1.49 2 1.59V8.96C5.278 8.73 4 7.81 4 6.155c0-1.504 1.19-2.49 3-2.62V2a.5.5 0 0 1 .5-.5zm-.5 2.033c-1.123.102-2 .695-2 1.622 0 .904.64 1.38 2 1.6V3.533zm1 7.947c1.05-.112 1.75-.754 1.75-1.66 0-.984-.605-1.543-1.75-1.77v3.91z"/>
                                                    </svg>
                                                </span>
                                                <span>@currency.Code</span>
                                            </h2>
                                            <div class="text-muted small">@currency.Name</div>
                                        </div>
                                        <div class="im-master-card-actions">
                                            <span class="badge rounded-pill im-status-badge @(isActive ? "im-status-badge--active" : "im-status-badge--inactive")" data-status-badge>@(isActive ? "Activo" : "Inactivo")</span>
                                        </div>
                                    </div>
                                    <form method="post" asp-page-handler="ToggleStatus" class="im-status-control">
                                        <input type="hidden" name="id" value="@currency.Id" />
                                        <input type="hidden" name="currentStatus" value="@currency.Status" />
                                        <button type="submit"
                                                class="im-status-switch @(isActive ? "is-active" : "is-inactive")"
                                                aria-label="Cambiar estado de la moneda"
                                                aria-pressed="@(isActive.ToString().ToLowerInvariant())">
                                            <span class="im-status-switch__thumb" aria-hidden="true"></span>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    }
                </div>

                <div class="d-none text-muted text-center py-4" data-empty-message>
                    No hay monedas que coincidan con los filtros aplicados.
                </div>
            </div>
        }
    </div>
</div>
